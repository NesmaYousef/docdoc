import 'package:flutter/material.dart';

class AppColors {
  // ── Brand (green — buttons, links, icons, active states) ──────────
  static const Color primary        = Color(0xFF0B6E4F);
  static const Color primaryLight   = Color(0xFF17B37A);
  static const Color primarySurface = Color(0xFFE8F5F0);

  // ── Text ──────────────────────────────────────────────────────────
  static const Color textDark       = Color(0xFF1C1C1E); // headings, body text
  static const Color textGray       = Color(0xFF6B7280); // captions, hints

  // ── Background ────────────────────────────────────────────────────
  static const Color bgSurface      = Color(0xFFF7F8FA); // scaffold background
  static const Color bgCard         = Color(0xFFFFFFFF); // cards, modals

  // ── Stroke ────────────────────────────────────────────────────────
  static const Color strokeGray     = Color(0xFFE5E7EB); // borders, dividers

  // ── Semantic ──────────────────────────────────────────────────────
  static const Color success        = Color(0xFF22C55E);
  static const Color warning        = Color(0xFFF59E0B);
  static const Color error          = Color(0xFFF43F5E);
}